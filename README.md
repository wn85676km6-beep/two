# 造师台

一个无需依赖的原型网站，用于创建自己的专属老师、选择学习方案，并和老师进行本地教学对话。

创建的老师会保存在当前浏览器的本地存储中。

## 本地运行

已提供零依赖本地服务：

```powershell
npm start
```

服务默认运行在 `http://localhost:3000`。

## 聊天功能

选择老师后点击“开始第一节课”即可打开聊天。服务端会调用 DeepSeek 智能老师；聊天记录保存在浏览器本地。API 密钥仅从运行服务的 `DEEPSEEK_API_KEY` 环境变量读取，绝不可写入前端或提交到仓库。
